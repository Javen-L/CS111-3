NAME: Anh Mac
EMAIL: anhmvc@gmail.com
UID: 905111606

lab4b.c: a C source file for an embedded application that builds and runs on an embedded system that gets readings from a temperature sensor connected to a BeagleBone board.

Makefile:
    ... default: build the lab4b program with the compiling option -Wall and -Wextra
    ... check: execute an automated smoke-test of the application to see if it runs and can talk to its sensors
    ... clean: delete all programs and output generated by the Makefile
    ... create the deliverable tarball with all files specified in this README

README: contains all descriptions of each of the included files.

smokeCheck.sh: contains all test cases for smoke check